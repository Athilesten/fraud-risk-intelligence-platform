global:
  scrape_interval: 5s

rule_files:
  - "/etc/prometheus/alert_rules.yml"

scrape_configs:
  - job_name: "fraud-fastapi"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["fraud-api:8000"]
